services:
  - type: web
    name: alexisplescia-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    preDeployCommand: npx prisma db push --accept-data-loss && npx tsx prisma/seed.ts
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
